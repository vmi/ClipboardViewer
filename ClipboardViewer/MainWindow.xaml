<Window x:Name="ClipboardViewer" x:Class="ClipboardViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClipboardViewer"
        mc:Ignorable="d"
        Title="Clipboard Viewer" Height="115" Width="800" Closed="ClipboardViewer_Closed" ContentRendered="ClipboardViewer_ContentRendered">
    <Window.Resources>
        <ContextMenu x:Key="menu">
            <MenuItem Header="Top Most" IsCheckable="True" Checked="MenuItem_Checked" Unchecked="MenuItem_Checked"/>
        </ContextMenu>
    </Window.Resources>
    <Grid>
        <RichTextBox x:Name="richTextBox" HorizontalAlignment="Left" VerticalScrollBarVisibility="Auto" IsReadOnly="True" ContextMenu="{DynamicResource menu}" UndoLimit="0">
            <RichTextBox.Resources>
                <Style TargetType="{x:Type Paragraph}">
                    <Setter Property="Margin" Value="0" />
                </Style>
            </RichTextBox.Resources>
            <FlowDocument/>
        </RichTextBox>

    </Grid>
</Window>
